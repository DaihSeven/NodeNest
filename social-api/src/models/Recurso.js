// src/models/Recurso.js
export default class Recurso {
    constructor(id, nome, tipo, endereco, descricao, contato) {
      this.id = id;
      this.nome = nome;
      this.tipo = tipo;
      this.endereco = endereco;
      this.descricao = descricao;
      this.contato = contato;
    }
  }
  